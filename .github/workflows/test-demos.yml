name: Demo Apps

on:
  push:
    branches:
      - master
  pull_request:
    paths:
      - "demos/**"
      - "src/**"

concurrency:
  group: demos-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  validate-demo:
    strategy:
      fail-fast: false
      matrix:
        demo:
          - name: fastapi-catalog
            dir: demos/fastapi-catalog
          - name: flask-blog
            dir: demos/flask-blog
          - name: ttl-sessions
            dir: demos/ttl-sessions
          - name: batch-importer
            dir: demos/batch-importer
    runs-on: ubuntu-latest
    name: ${{ matrix.demo.name }}
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ${{ matrix.demo.dir }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync

      - name: Syntax check (py_compile)
        run: |
          find . -name '*.py' -print -exec uv run python -m py_compile {} \;
